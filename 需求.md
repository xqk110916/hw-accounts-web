| 任务管理 | 入库管理     | 1）添加入库任务，”金属量%”改为”百分比含量”，该数值是从材料管理的“百分比含量”取值回显，该值可以修改；然后将该字段的位置放到毛皮净后面<br />2）在“百分比含量”字段后增加”元素量“字段，列表和编辑弹窗中都需要添加；该字段需要自动计算得来：元素量=百分比含量*净重，把这两个字段放到毛、皮净下面；同时excel导入的时候显示的预览表格也要做相应调整； |
| -------- | ------------ | ------------------------------------------------------------ |
|          |              | 添加任务：出库时间去掉时分秒                                 |
|          |              | 添加任务-手动选择：右侧”已选容器”后面显示所有净重的汇总值；  |
|          | 实物盘存     | 10）.PC端的，把盘盈盘亏去掉，表单显示正常数量和不正常数量，表单中不正常容器字段去掉，列表中如果是不正常用红色背景显示 <br />11）录入盘存结果时，手动修改结果，弹框二次确认。 |
| 库房管理 | 位置图管理   | 1）3D货架 中将容器改成正方体（50\*50\*50），然后新库房货架的平面改为 70\* 70，高度 65；老库房平面也改为 70\*70。 <br />2）老库房的二维平面图做成正方形，高度不需要改变，只修改宽度和目前的高度一致就行； <br /><br />3）<br />5）如果是老库房，每个区的列从1开始，如ABC三个区，B区的第一列为B1-1 6）把位置S标识都 去掉，如原先是S1-1-1改为1-1-1，如果是老区的为A1-1 7）如果库房里有待确认或者已添加的货物，当删除库房时，提示不让删除 |
| 数据管理 | /            | 列表中只要有位置的数据，都要显示对应的库房编号               |
|          | 标签模版管理 | 标签模版管理：时间去掉时分秒；                               |
|          | 材料管理     | 材料管理-添加元素：把含量改为”百分比含量”，R01和R03中的”元素量-百分比含量”取自该值，“元素量”=“百分比含量”*材料量净重。只能添加一条元素。 |

