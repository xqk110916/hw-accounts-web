你是一个经验极其丰富的前端开发工程师，精通 Vue 2 + Element-UI。

现在有一个墨刀高保真原型，地址是：
https://modao.cc/proto/fGlGgHutbke2f1O1gBKbd/sharing?view_mode=read_only

请先完整分析这个原型的“数据管理”菜单下的所有页面（包括所有页面、布局、组件、交互逻辑、动画、状态切换、响应式效果等）。

目前没有后端服务，路由直接添加在localUserInfo.js 中的假数据即可，不需要发送后端请求，不需要调用“菜单管理技能”。

界面风格和代码风格参照 “src/views/task” 中的界面来进行修改

**严格要求：**
1. **代码风格必须 100% 匹配我本地现有项目**  
   （我会把本地项目的关键代码片段 / 组件示例 / 配置文件一起上传给你，请严格按照里面的：
   - 命名规范（变量、方法、文件命名）
   - 代码缩进、换行风格
   - 组件拆分方式、Props 和 Event 的写法
   - 样式写法（是否使用 scoped、Less/SCSS、变量命名等）
   - 路由、状态管理、工具函数的使用习惯
   等全部风格来开发）

2. **技术栈要求：**
   - Vue 2.x + Element-UI（必须使用 Element-UI 的所有组件，不要用 Element Plus）
   - 完整项目结构（src/components、views、router、assets 等）
   - 使用 Vue CLI / Vite 的标准写法（根据我本地风格决定）
   - 所有交互、表单校验、表格、分页、弹窗、Tabs 等必须使用 Element-UI 原生组件实现

3. **交付内容：**
   - 完整可直接运行的前端项目代码（请按文件夹结构给出）
   - 所有页面必须实现原型里的交互和视觉效果
   - 代码必须干净、可维护、注释清晰
   - 如果是CRUD类型的界面, 请参照我 src\views\demo\crud 中的界面来实现;

请先确认你已经看懂原型，然后一步步输出：
1. 先告诉我你分析出的原型主要页面和核心交互点
2. 再给出完整项目代码

现在开始，请严格按照以上要求开发。