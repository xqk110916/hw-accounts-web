完成"出库管理"界面的修改:
1. 完善出库管理的筛选条件, 参照入库管理界面中的筛选条件, 仅将"入库时间"修改为"出库时间";
2. 表单界面字段不需要修改保持原状, 操作按钮的权限控制和入库管理保持一致(例如: 什么状态显示什么按钮), 额外添加一种状态, 暂存 dataStatus=4 该状态同未审核的状态允许编辑;
3. 调拨依据 不需要修改;
4. "添加出库任务"弹窗中, 明细信息的"手动选择"按钮操作修改, 去除之前的"明细编辑"弹窗, 弹窗改为"选择容器";
  4-1. "选择容器"弹窗中, 分为左右两侧的内容, 左侧用于选择容器(宽度70%), 右侧用于显示当前选择的容器列表(宽度30%);
  4-2. 右侧的容器列表以table格式展示, 字段显示容器号, 任务编号, 净重; 同时可以将容器从列表中移除;
  4-3. 左侧选择容器时, 顶部三个tab选项, 可以按照["位置图", "容器列表", "入库单"] 三种维度进行筛选展示容器以供选择, 选择时可多选;
  位置图: 先选择对应的平衡区+库房后(用tag显示, 点选平衡区后, 根据接口请求对应的库房, 然后选择完库房之后再进行查询对应的容器), 根据库房查询对应容器接口(/busin/locationMap/positionMap); 展示容器的时候按照"位置图管理"界面查看对应库房位置图的形式展示(仅展示2D), 然后选择对应的容器可以弹出详情弹窗(详情弹窗展示库房管理中的就行), 并且在详情弹窗中可以将其加入选择列表;
  容器列表: 展示一个table列表(table字段和明细信息列表保持一致), 展示所有的容器; 接口(busin/inbound/goodsPageList); 筛选条件添加"容器号", "货物编码", "入库时间(区间)";
  入库单列表: 展示一个table列表(table字段 第一个字段为"任务编号": taskNum; 其余字段和明细信息列表保持一致), 展示所有的容器; 接口(busin/inbound/goodsPageList); 筛选条件"任务编号", 但是在筛选时需要查询(busin/inbound/getByTaskNumAndContainerCode) 接口, 清除筛选条件后还正常请求(busin/inbound/goodsPageList)接口;
5. 自动配置先保持原状;
6. 其他出库管理相关接口, 根据接口文档进行接口对接(/md/出库管理-取货方案接口文档.md)


以上接口可以在/md中找到对应的接口文档进行查找和查看详情;