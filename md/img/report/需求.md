1. 查看"菜单管理"技能, 添加"表单管理"菜单,同时在该菜单中添加"表单报送"界面, 在代码中创建表单管理的代码; 
2. 界面展示一个card组, 以card的形式显示 R01-R09, 点击切换到详情界面, 详情界面表头显示分为左右两侧显示, 左侧: "返回" 按钮(点击返回的card界面), 右侧: "保存", "导出", "打印"; 然后再显示一个筛选项配置区域, 然后根据当前编号显示报表内容 @[md/img/report/report_templates.md]  , 最下方显示一个table列表; 


表单报送界面:
1. R01:
   1. 筛选区域: 任务类型(select 出库/入库), 日期区间选择器, 选择任务(select) 后面添加一个按钮"接入报表", 从历史中选择(select 多选 另起一行显示), 
   2. 列表区域: 序号, 批号, 每批物件数, 容器编号, 材料转移类型, 材料代码, 材料类型代码, 重量单位, 材料量(多级表头, 包含字段: 毛重, 皮重, 净重), 元素量(多级表头, 包含字段: 元素代码, 百分比含量, 元素量), 同位素量(多级表头, 包含字段: 同位素代码, 百分比含量, 同位素量), 测量点代码, 测量情况, 注释符
2. R03:
   1. 筛选区域: 选择季度(两个选择器: 1. 年份选择器：el-date-picker type="years"; 2. 季度选择器: 直接使用select, 选项写死 第一/二/三/四季度) 后面添加一个按钮"统计"; 从历史中选择(select 单选 另起一行显示)
   2. 列表区域: 序号, 库存变化日期, 单位或平衡区(多级表头, 包含字段: 来处, 去除), 库存变化代码, 批号, 每批物件数, 材料代码, 材料类型代码, 重量单位, 材料量(多级表头, 包含字段: 毛重, 皮重, 净重), 元素量(多级表头, 包含字段: 元素代码, 百分比含量, 元素量), 同位素量(多级表头, 包含字段: 同位素代码, 百分比含量, 同位素量), 测量点代码, 测量情况, 注释符
3. R04:
   1. 筛选区域: 选择季度(两个选择器: 1. 年份选择器：el-date-picker type="years"; 2. 季度选择器: 直接使用select, 选项写死 第一/二/三/四季度) 后面添加一个按钮"统计"; 从历史中选择(select 单选 另起一行显示)
   2. 列表区域: 序号, 测量点代码, 平衡区代码, 批号, 每批物件数, 材料代码, 材料类型代码, 重量单位, 材料量(多级表头, 包含字段: 毛重, 皮重, 净重), 元素量(多级表头, 包含字段: 元素代码, 百分比含量, 元素量), 同位素量(多级表头, 包含字段: 同位素代码, 百分比含量, 同位素量), 测量情况, 注释符
4. R05:
   1. 筛选区域: 选择季度(两个选择器: 1. 年份选择器：el-date-picker type="years"; 2. 季度选择器: 直接使用select, 选项写死 第一/二/三/四季度) 后面添加一个按钮"统计"; 从历史中选择(select 单选 另起一行显示)
   2. 列表区域: 序号, 平衡区(多级表头, 包含字段: 编号, 名称), 衡算数据(多级表头, 包含字段: 衡算对象名称, 丰度(%)), 登记项目(多级表头, 包含字段: 类别代码, 名称), 衡算数据(多级表头, 包含字段: 物素代码, 元素代码, 同位素代码, 重量, 重量单位), 注释符
5. R06:
   1. 筛选区域: 查询日期(日期区间) 后面跟一个按钮"统计"; 从历史中选择(select 单选 另起一行显示)
   2. 列表区域: 序号, 平衡区, 材料代码, 通用名称, 批号, 容器号, 变化码, 注释内容, 操作(新增/删除)
6. R08:
   1. 筛选区域: 统计日期(日期区间), 选择材料(select 多选) 后面跟一个按钮"生成";  从历史中选择(select 单选 另起一行显示)
   2. 列表区域: 序号, 平衡区代码, 物素名称, 同位素含量(%), 重量单位, 本结算周期内材料变动量(多级表头, 包含字段: 期初, 调入, 调出, 产出, 消耗, 其他增量, 其他减量, 已知损失, 期末账面), 注释符, 备注
7. R09:
   1. 筛选区域: 统计日期(日期区间), 选择材料(select 多选) 后面跟一个按钮"生成";  从历史中选择(select 单选 另起一行显示)
   2. 列表区域: 序号, 平衡区代码, 物素名称, 同位素含量(%), 重量单位, 本结算周期内材料变动量(多级表头, 包含字段: 本期末账面, 调整后账面, 本期末实物, 不平衡差, 总损失率), 注释符, 备注

"打印"按钮功能, 将报表区域调用浏览器打印, 只打印该区域的内容