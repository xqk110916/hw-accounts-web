存量报表：
1. 筛选条件：库房（select多选），材料类型（材料管理中的数据接口：busin/material-code/listAll），生产单位（输入框），入库时间（日期区间）
2. 功能按钮：导出
3. 列表：tab切换 汇总/明细；同时“汇总”table时，需要显示一个汇总项（调用接口中的“汇总统计”接口，显示字段 "容器总数"（totalContainerCount），"总重量"（totalNetWeight） ）
   1. 汇总table字段：库房，材料代码，批号，生产单位，入库时间，数量，重量（毛皮净），操作（需要支持一个“详情”按钮）。
   2. 明细table字段：库房，容器号，材料代码，批号，生产单位，入库时间，位置，封记编码，重量（毛皮净），操作（需要支持一个“详情”按钮）。

变化量报表：
1. 筛选条件：库房（select多选），材料类型（材料管理中的数据接口：busin/material-code/listAll），生产单位（输入框），出入库时间（日期区间），类型（选项，出库/入库）
2. 功能按钮：导出
3. 列表：tab切换 汇总/明细；同时“汇总”table时，需要显示一个汇总项（调用接口中的“汇总统计”接口，显示字段 "总入库"：`${inboundCount}（${inboundWeight}）`，“总出库”：`${outboundCount}（outboundWeight）` ）
   1. 汇总table字段：库房，材料代码，类型，批号，出入库时间，数量，重量（毛皮净），操作（需要支持一个“详情”按钮）。
   2. 明细table字段：任务编号，容器号，材料代码，生产单位，出入库时间，位置，封记编码，重量（毛皮净）。

账目管理：
1. 进来分为5个tab显示界面：总账，总账明细，库房账明细，生产单位明细账，材料账明细。
2. 筛选条件：
   1. 总账：年份（年份选择器：el-date-picker type="years"），材料（多选，材料管理中的数据接口：busin/material-code/listAll），库房
   2. 总账明细：时间（日期区间），材料（多选，材料管理中的数据接口：busin/material-code/listAll），库房（选项）
   3. 库房账明细：时间（日期区间），材料（多选，材料管理中的数据接口：busin/material-code/listAll），库房（选项）
   4. 生产单位明细账：时间（日期区间），材料（多选，材料管理中的数据接口：busin/material-code/listAll），生产单位（输入）
   5. 材料账明细：时间（日期区间），材料（多选，材料管理中的数据接口：busin/material-code/listAll）
3. 功能按钮：导出
4. 列表：
   1. 总账：显示标题：xxxx年总账，列表显示字段：材料代码，生产单位，库房，件数，重量
   2. 总账明细：分为上下两部分显示“汇总”和“明细”。
      1. 汇总：日期，类型，库房，调拨依据，材料代码，调入（多级表头，包含字段：调入单位，件数，材料量），调出（多级表头，包含字段：调出单位，件数，材料量），结存（多级表头，包含字段：件数，材料量）。
      2. 明细：任务编号，容器号，材料代码，生产单位，出入库时间，位置，封记编码，重量（毛皮净）。
   3. 库房账明细：分为上下两部分显示“汇总”和“明细”。
      1. 汇总：显示标题“xxx库房总账”，列表显示字段：日期，类型，库房，调拨依据，材料代码，调入（多级表头，包含字段：调入单位，件数，材料量），调出（多级表头，包含字段：调出单位，件数，材料量），结存（多级表头，包含字段：件数，材料量）。
      2. 明细：任务编号，容器号，材料代码，生产单位，出入库时间，位置，封记编码，重量（毛皮净）。
   4. 生产单位明细：分为上下两部分显示“汇总”和“明细”。
      1. 汇总：显示标题“xxx生产单位总账”，列表显示字段：日期，类型，库房，调拨依据，材料代码，调入（多级表头，包含字段：调入单位，件数，材料量），调出（多级表头，包含字段：调出单位，件数，材料量），结存（多级表头，包含字段：件数，材料量）。
      2. 明细：任务编号，容器号，材料代码，生产单位，出入库时间，位置，封记编码，重量（毛皮净）。
   5. 材料账明细：分为上下两部分显示“汇总”和“明细”。
      1. 汇总：显示标题“xxx材料总账”，列表显示字段：日期，类型，库房，调拨依据，材料代码，调入（多级表头，包含字段：调入单位，件数，材料量），调出（多级表头，包含字段：调出单位，件数，材料量），结存（多级表头，包含字段：件数，材料量）。
      2. 明细：任务编号，容器号，材料代码，生产单位，出入库时间，位置，封记编码，重量（毛皮净）。

综合查询：
1. 筛选条件字段和列表字段保持不变，但是目前这个界面目前都是假数据，需要根据字段去真实请求数据获取选项。类型字段改为select选项（已出库、库存）
2. 列头管理功能的实现。