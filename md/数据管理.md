**【角色设定】**
你是一位资深的产品经理兼高级前端架构师，拥有极强的视觉解析能力和将 UI 设计转化为前端组件化需求的经验。

**【任务目标】**
我为你上传了一系列原型图的截图。请仔细分析这些界面的视觉布局、字段信息和潜在交互，将其转化为结构化、可落地的《前端开发需求文档》。

**【全局背景与核心约束】**
1. **模块归属**：所有的界面均属于系统中的“数据管理”菜单。
2. **界面命名**：每一张图片的**文件名即为该界面的准确名称**，请在文档中严格按照文件名分模块进行需求描述。
3. **UI/UX 一致性（最高优先级）**：本模块的所有界面风格、交互逻辑、组件形态（如按钮规范、表格样式、表单对齐方式、弹窗动画等）**必须严格与已有目录 `src/views/task` 中的界面保持完全一致**。在需求描述中，必须将“复用 task 模块交互规范”作为最高准则。
4. **技术视角**：请基于 Vue3 组合式 API 和主流组件库（如 Element-plus）的思维来拆解页面结构。

**【执行步骤】**
对于我上传的每一张图片，请执行以下分析：
1. 识别并拆解页面宏观布局（如：顶部查询筛选区、核心数据表格区、底部侧边栏/抽屉等）。
2. 提取所有可见的表单字段、表格列名、操作按钮及交互状态。
3. 推导潜在的业务与交互逻辑（如：必填校验、下拉框联动、按钮的触发事件及弹窗反馈）。

**【输出格式要求】**
请使用 Markdown 格式输出文档，保持语言简洁专业。请在每个界面的标题下方，使用 Markdown 图片语法插入该界面的原图，图片相对路径统一固定为 `md/img/[图片完整文件名及后缀]`。结构必须严格如下：

# 模块开发需求：数据管理

## 1. 全局设计与架构规范
* **UI/UX 基准**：明确声明与 `src/views/task` 的一致性要求，并推测可直接复用的组件形态（如：统一的查询表单组件、分页表格组件等）。

## 2. 界面需求细化（按图片逐一展开）
### 界面：[此处填写图片的文件名，不含后缀]
![界面原图](md/img/[此处填写完整的图片文件名及后缀])

* **界面概述**：该界面的核心业务目标。
* **视图结构拆解**：分析页面由哪些核心前端区段构成。
* **数据元素与校验清单**：
    * **查询条件**：列出所有搜索字段、控件类型（输入框/日期/下拉等）及默认状态。
    * **数据展示**：列出表格或列表的所有字段列名、特殊排版格式或状态标签。
* **操作与交互逻辑**：
    * 列举所有交互按钮（新增、编辑、删除、导出等）。
    * 详细说明点击后的预期交互反馈（如：触发路由跳转、打开抽屉表单、弹出二次确认对话框等）。